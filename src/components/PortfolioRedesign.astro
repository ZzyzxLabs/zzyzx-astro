<main class="portfolio-shell overflow-hidden">
  <section class="portfolio-hero" aria-labelledby="portfolio-title">
    <div class="portfolio-hero__stage">
      <header class="portfolio-hero__copy">
        <p class="portfolio-label">Portfolio</p>
        <h1 id="portfolio-title">Built for real use.</h1>
        <p class="portfolio-hero__intro">
          Selected systems across AI agents, smart accounts, payments, and onchain infrastructure.
        </p>
        <div class="portfolio-hero__actions">
          <a href="#cases" class="portfolio-button-primary">View portfolio</a>
          <a href="mailto:contact@zzyzx.com?subject=Start%20a%20brief" class="portfolio-button-secondary">Start a brief</a>
        </div>
      </header>

    </div>
  </section>

  <section id="cases" class="portfolio-anchor scroll-mt-20">
    <div class="portfolio-container">
      <div class="portfolio-anchor__grid" data-animate="fade-up">
        <div>
          <h2 class="portfolio-case-title">Nereus</h2>
          <p class="portfolio-case-lead">
            A prediction market using AI agents in trusted execution environments for decentralized outcome resolution.
          </p>
          <a href="https://trynereus.xyz" class="portfolio-text-link">Visit Nereus <span aria-hidden="true">↗</span></a>
        </div>

        <dl class="portfolio-meta">
          <div>
            <dt>Focus</dt>
            <dd>AI agents and trusted execution</dd>
          </div>
          <div>
            <dt>System</dt>
            <dd>Prediction market</dd>
          </div>
          <div>
            <dt>Environment</dt>
            <dd>Web3 product</dd>
          </div>
        </dl>
      </div>

      <figure class="portfolio-nereus__media" data-animate="fade-up">
        <img
          src="/nereus-case.webp"
          alt="Nereus landing page with a dark ocean-themed interface and the headline Predict the Unknowable"
          width="1737"
          height="933"
          loading="eager"
          fetchpriority="high"
        />
      </figure>
    </div>
  </section>

  <section class="portfolio-seawallet">
    <article class="portfolio-container portfolio-seawallet__grid">
      <figure class="portfolio-seawallet__media" data-animate="fade-up">
        <img
          src="/seawallet-case.webp"
          alt="SeaWallet launch artwork with a pixel ocean and the SeaWallet.ai wordmark"
          width="1456"
          height="816"
          loading="lazy"
        />
      </figure>

      <div class="portfolio-seawallet__copy" data-animate="fade-up">
        <p class="portfolio-category">Smart account infrastructure</p>
        <h2 class="portfolio-case-title">SeaWallet</h2>
        <p>
          A smart contract account platform for customizable inheritance and asset distribution.
        </p>
        <a href="https://SeaWallet.ai" class="portfolio-text-link">Visit SeaWallet <span aria-hidden="true">↗</span></a>
      </div>
    </article>
  </section>

  <section class="portfolio-seapay">
    <article class="portfolio-container" data-animate="fade-up">
      <p class="portfolio-category">Agent-ready payments</p>
      <div class="portfolio-seapay__grid">
        <div>
          <h2 class="portfolio-case-title portfolio-case-title--large">SeaPay</h2>
          <p class="portfolio-case-lead">
            A hotel payment gateway integrating x402 and A2A standards for AI-led travel booking.
          </p>
        </div>

        <div>
          <dl class="portfolio-meta">
            <div>
              <dt>Focus</dt>
              <dd>Agent-ready payments</dd>
            </div>
            <div>
              <dt>Standards</dt>
              <dd>x402 and A2A</dd>
            </div>
            <div>
              <dt>System</dt>
              <dd>Hotel payment gateway</dd>
            </div>
          </dl>
          <a href="https://SeaPay.ai" class="portfolio-text-link">Visit SeaPay <span aria-hidden="true">↗</span></a>
        </div>
      </div>
    </article>
  </section>

  <section class="portfolio-more" aria-label="More selected work">
    <div class="portfolio-container portfolio-more__grid">
      <article class="portfolio-project portfolio-project--supwallet" data-animate="fade-up">
        <div>
          <p class="portfolio-project__category">Agent economy</p>
          <h2 class="portfolio-project__title portfolio-project__title--large">Sup Wallet</h2>
        </div>
        <div class="portfolio-project__body">
          <p>
            A self-custodial Sui wallet that gives AI agents and services typed, revocable allowances without surrendering control.
          </p>
          <a href="https://www.supwallet.app/" class="portfolio-text-link">Visit Sup Wallet <span aria-hidden="true">↗</span></a>
        </div>
      </article>

      <article class="portfolio-project portfolio-project--thru" data-animate="fade-up">
        <p class="portfolio-project__category">Layer 1 infrastructure</p>
        <h2 class="portfolio-project__title">Thru</h2>
        <p class="portfolio-project__description">
          A language-agnostic Layer 1 built on a RISC-V runtime for C, C++, Rust, and other LLVM languages.
        </p>
        <a href="https://thru.org/" class="portfolio-text-link">Visit Thru <span aria-hidden="true">↗</span></a>
      </article>

      <article class="portfolio-project portfolio-project--broker" data-animate="fade-up">
        <p class="portfolio-project__category">Digital asset brokerage</p>
        <h2 class="portfolio-project__title">Broker Finance</h2>
        <p class="portfolio-project__description">
          A digital brokerage experience for accessing crypto and tokenized assets through one client platform.
        </p>
        <a href="https://broker.finance/" class="portfolio-text-link">Visit Broker Finance <span aria-hidden="true">↗</span></a>
      </article>
    </div>
  </section>

  <section class="portfolio-close">
    <div class="portfolio-container portfolio-close__grid" data-animate="fade-up">
      <h2>Have a Web3 or AI product to build?</h2>
      <a href="mailto:contact@zzyzx.com?subject=Start%20a%20brief" class="portfolio-button-primary">Start a brief</a>
    </div>
  </section>
</main>

<style>
  .portfolio-shell {
    background: #08090a;
    color: #f1f3f0;
  }

  .portfolio-container {
    width: min(100%, 88rem);
    margin-inline: auto;
  }

  .portfolio-hero {
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    padding: 4.25rem 1.25rem 0;
  }

  .portfolio-hero__stage {
    position: relative;
    display: grid;
    min-height: calc(100dvh - 4.25rem);
    max-width: 88rem;
    margin-inline: auto;
    grid-template-columns: repeat(12, minmax(0, 1fr));
    align-items: end;
    padding-bottom: clamp(4.5rem, 10vh, 7.5rem);
  }

  .portfolio-hero__copy {
    position: relative;
    grid-column: 1 / 8;
  }

  .portfolio-label {
    font-size: 0.7rem;
    font-weight: 500;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: #74d7dd;
  }

  .portfolio-hero h1 {
    max-width: 9ch;
    margin-top: 1.75rem;
    font-family: var(--font-heading);
    font-size: clamp(3.2rem, 5.5vw, 5rem);
    font-weight: 500;
    line-height: 0.97;
    letter-spacing: -0.058em;
    color: #f1f3f0;
  }

  .portfolio-hero__intro {
    max-width: 32rem;
    margin-top: 1.75rem;
    font-size: 1.0625rem;
    line-height: 1.75;
    color: #a0a7ac;
  }

  .portfolio-hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 2.25rem;
  }

  .portfolio-button-primary,
  .portfolio-button-secondary {
    display: inline-flex;
    min-height: 3rem;
    align-items: center;
    justify-content: center;
    border: 1px solid;
    border-radius: 2px;
    padding: 0.75rem 1.25rem;
    white-space: nowrap;
    font-size: 0.86rem;
    font-weight: 600;
    text-decoration: none;
    transition: background-color 220ms ease, border-color 220ms ease, color 220ms ease, transform 220ms ease;
  }

  .portfolio-button-primary {
    border-color: #74d7dd;
    background: #74d7dd;
    color: #071012;
  }

  .portfolio-button-secondary {
    border-color: rgba(242, 243, 239, 0.22);
    background: rgba(8, 9, 10, 0.34);
    color: #dfe2df;
  }

  .portfolio-button-primary:hover,
  .portfolio-button-secondary:hover {
    transform: translateY(-1px);
  }

  .portfolio-button-primary:hover {
    border-color: #a3e6e9;
    background: #a3e6e9;
  }

  .portfolio-button-secondary:hover {
    border-color: rgba(116, 215, 221, 0.68);
    color: #a3e6e9;
  }

  .portfolio-anchor {
    background: #0b0d0f;
    padding: 7rem 1.25rem;
  }

  .portfolio-anchor__grid {
    display: grid;
    gap: 3rem;
  }

  .portfolio-nereus__media {
    margin: clamp(3.5rem, 7vw, 6rem) 0 0;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.11);
    background: #08090a;
  }

  .portfolio-nereus__media img {
    display: block;
    width: 100%;
    height: auto;
  }

  .portfolio-case-title {
    font-family: var(--font-heading);
    font-size: clamp(2.75rem, 4.8vw, 4.25rem);
    font-weight: 500;
    line-height: 1;
    letter-spacing: -0.055em;
    color: #edf0ec;
  }

  .portfolio-case-title--large {
    font-size: clamp(3.4rem, 7vw, 6rem);
  }

  .portfolio-case-lead {
    max-width: 42rem;
    margin-top: 1.75rem;
    font-size: clamp(1.15rem, 1.7vw, 1.45rem);
    line-height: 1.6;
    color: #a4abb0;
  }

  .portfolio-meta {
    margin: 0;
    border-top: 1px solid rgba(255, 255, 255, 0.11);
  }

  .portfolio-meta > div {
    display: grid;
    grid-template-columns: 6.5rem 1fr;
    gap: 1rem;
    padding: 1rem 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  .portfolio-meta dt {
    color: #697279;
    font-size: 0.78rem;
  }

  .portfolio-meta dd {
    margin: 0;
    color: #b8bec2;
    font-size: 0.88rem;
  }

  .portfolio-text-link {
    display: inline-flex;
    width: fit-content;
    align-items: center;
    gap: 0.7rem;
    margin-top: 2rem;
    border-bottom: 1px solid rgba(116, 215, 221, 0.45);
    padding-bottom: 0.2rem;
    color: #9de2e6;
    font-size: 0.88rem;
    font-weight: 600;
    text-decoration: none;
    transition: border-color 220ms ease, color 220ms ease;
  }

  .portfolio-text-link:hover {
    border-color: #c1f0f2;
    color: #c1f0f2;
  }

  .portfolio-seawallet {
    padding: 6rem 1.25rem;
  }

  .portfolio-seawallet__grid {
    display: grid;
    align-items: start;
    gap: 3rem;
  }

  .portfolio-seawallet__media {
    margin: 0;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.11);
    background: #0b0d0f;
  }

  .portfolio-seawallet__media img {
    display: block;
    width: 100%;
    height: auto;
    transition: transform 600ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .portfolio-seawallet__media:hover img {
    transform: scale(1.01);
  }

  .portfolio-seawallet__copy {
    padding-top: 0.5rem;
  }

  .portfolio-category {
    color: #798188;
    font-size: 0.82rem;
  }

  .portfolio-seawallet__copy .portfolio-case-title {
    margin-top: 1rem;
  }

  .portfolio-seawallet__copy > p:not(.portfolio-category) {
    max-width: 34rem;
    margin-top: 1.5rem;
    color: #929aa0;
    line-height: 1.75;
  }

  .portfolio-seapay {
    border-block: 1px solid rgba(255, 255, 255, 0.1);
    background: #0b0d0f;
    padding: 6rem 1.25rem;
  }

  .portfolio-seapay__grid {
    display: grid;
    gap: 3rem;
    margin-top: 1.5rem;
  }

  .portfolio-more {
    padding: 6rem 1.25rem;
  }

  .portfolio-more__grid {
    display: grid;
    grid-template-columns: repeat(12, minmax(0, 1fr));
  }

  .portfolio-project {
    min-width: 0;
  }

  .portfolio-project--supwallet {
    display: grid;
    min-height: 31rem;
    grid-column: 1 / 8;
    grid-row: 1 / 3;
    align-content: space-between;
    padding-right: clamp(2.5rem, 5vw, 5rem);
  }

  .portfolio-project--thru,
  .portfolio-project--broker {
    grid-column: 8 / -1;
    border-left: 1px solid rgba(255, 255, 255, 0.1);
    padding-left: clamp(2rem, 4vw, 4rem);
  }

  .portfolio-project--thru {
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    padding-bottom: 3.5rem;
  }

  .portfolio-project--broker {
    padding-top: 3.5rem;
  }

  .portfolio-project__category {
    color: #798188;
    font-size: 0.82rem;
  }

  .portfolio-project__title {
    max-width: 12ch;
    margin-top: 1rem;
    font-family: var(--font-heading);
    font-size: clamp(2.75rem, 4.5vw, 4.25rem);
    font-weight: 500;
    line-height: 0.98;
    letter-spacing: -0.055em;
    color: #edf0ec;
  }

  .portfolio-project__title--large {
    max-width: 8ch;
    font-size: clamp(4rem, 8vw, 7.5rem);
  }

  .portfolio-project__body,
  .portfolio-project__description {
    max-width: 34rem;
    color: #929aa0;
    line-height: 1.75;
  }

  .portfolio-project__description {
    margin-top: 1.5rem;
  }

  .portfolio-close {
    padding: 6rem 1.25rem;
  }

  .portfolio-close__grid {
    display: grid;
    gap: 2.5rem;
    align-items: end;
  }

  .portfolio-close h2 {
    max-width: 17ch;
    font-family: var(--font-heading);
    font-size: clamp(2.6rem, 4.8vw, 4.2rem);
    font-weight: 500;
    line-height: 1.04;
    letter-spacing: -0.052em;
    color: #edf0ec;
  }

  .portfolio-button-primary:focus-visible,
  .portfolio-button-secondary:focus-visible,
  .portfolio-text-link:focus-visible {
    outline: 2px solid #74d7dd;
    outline-offset: 4px;
  }

  @media (prefers-reduced-motion: no-preference) {
    .portfolio-hero__copy {
      animation: portfolio-copy-in 820ms cubic-bezier(0.22, 1, 0.36, 1) both;
    }

  }

  @keyframes portfolio-copy-in {
    from { opacity: 0; transform: translateY(16px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @media (min-width: 640px) {
    .portfolio-hero,
    .portfolio-anchor,
    .portfolio-seawallet,
    .portfolio-seapay,
    .portfolio-more,
    .portfolio-close {
      padding-inline: 1.75rem;
    }
  }

  @media (min-width: 768px) {
    .portfolio-anchor__grid {
      grid-template-columns: minmax(0, 1.3fr) minmax(18rem, 0.7fr);
      align-items: end;
    }

    .portfolio-seawallet__grid {
      grid-template-columns: minmax(0, 7fr) minmax(18rem, 4fr);
      gap: 4rem;
    }

    .portfolio-seapay__grid {
      grid-template-columns: minmax(0, 7fr) minmax(18rem, 4fr);
      gap: 4rem;
    }

    .portfolio-close__grid {
      grid-template-columns: minmax(0, 1fr) auto;
    }
  }

  @media (min-width: 1024px) {
    .portfolio-hero,
    .portfolio-anchor,
    .portfolio-seawallet,
    .portfolio-seapay,
    .portfolio-more,
    .portfolio-close {
      padding-inline: 2.5rem;
    }

    .portfolio-anchor,
    .portfolio-seawallet,
    .portfolio-seapay,
    .portfolio-more,
    .portfolio-close {
      padding-block: 7rem;
    }
  }

  @media (max-width: 767px) {
    .portfolio-hero {
      padding-top: 4.25rem;
    }

    .portfolio-hero__stage {
      display: flex;
      min-height: calc(100dvh - 4.25rem);
      align-items: flex-end;
      padding-top: 2.5rem;
      padding-bottom: 4.5rem;
    }

    .portfolio-hero h1 {
      font-size: clamp(2.9rem, 13vw, 3.65rem);
    }

    .portfolio-hero__actions {
      flex-direction: column;
    }

    .portfolio-button-primary,
    .portfolio-button-secondary {
      width: 100%;
      min-height: 3rem;
    }

    .portfolio-meta > div {
      grid-template-columns: 5.5rem 1fr;
    }

    .portfolio-more__grid {
      display: block;
    }

    .portfolio-project--supwallet,
    .portfolio-project--thru,
    .portfolio-project--broker {
      display: block;
      min-height: 0;
      border-left: 0;
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
      padding: 2.75rem 0;
    }

    .portfolio-project--supwallet {
      padding-top: 0;
    }

    .portfolio-project--broker {
      border-bottom: 0;
      padding-bottom: 0;
    }

    .portfolio-project__body {
      margin-top: 2rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .portfolio-button-primary,
    .portfolio-button-secondary,
    .portfolio-text-link,
    .portfolio-seawallet__media img {
      transition: none;
    }

    .portfolio-seawallet__media:hover img {
      transform: none;
    }

    .portfolio-shell :global([data-animate]) {
      opacity: 1 !important;
      transform: none !important;
      transition: none !important;
    }
  }
</style>
